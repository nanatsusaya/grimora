// Fixture: an innocent second plugin, used only as the *target* of an illegal plugin→plugin import
// from `plugins/evil` (see ../../evil/src/imports-plugin.ts). Never shipped.
export const otherPluginValue = 'other';
